import React from 'react';

import './utils.scss';

/* eslint-disable-next-line */
export interface UtilsProps {}

export const Utils = (props: UtilsProps) => {
  return (
    <div>
      <h1>Welcome to utils component!</h1>
    </div>
  );
};

export default Utils;
